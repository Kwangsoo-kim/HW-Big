//exception
//예외처리는 try~ catch문으로 처리한다. 되도록 적은 범위의 블록을 감싸 예외처리 부분을
최소화 하는편이 프로그램의 실행에 이롭다.
//일반적으로 try문안에 try문을 중복으로 넣지 않는다.대신 catch절을 다중으로 넣음으로 해결한다.
//catch절이 다수일때 맨 위에는 Exception을 상속받는 자손에서부터 부모순으로 배열해야한다.
//finally문은 예외가 실행되든 안되든 무조건 실행되는 문.
//throws
//throws의 경우 예외가 발생할문구를 호출한 쪽으로 예외를 던지는 방식.
//점점 위로던지다가 클래스에 까지 던지면 메인에서 던져야 실행되는데, 결국 같은 에러가 발생한다.
//throws의 경우 강제로 예외를 발생시켜 위로 던져, main문에서 try~catch문구를 추가하여
예외처리하는 방식에서 주로 사용한다.