// if

	1. if~else 문장 : 조건식을 이용해 단독으로 사용가능. 경우에 따라 else if 블록으로 
		추가 조건식을 정할 수 있고, 만족하는 조건식을 찾지 못할 때 실행되는
		else 블록을 가질 수 있다.

	2. ifelse() 함수
		- Java의 삼항연산자와 비슷한 용도로 사용
		ex) ifelse(조건식, true값 , false값) 

	3. switch() 함수
		- Java의 switch와 비슷한 구문으로 사용된다
		ex) switch(idx,list) 
// 210203 kks